/// @description  Draw text
if(room==rm_scoreboard)draw_text(x-1, y+3, string_hash_to_newline("Classic Mode"));
if(room==rm_scoreboard_hr)draw_text(x-1, y+3, string_hash_to_newline("Hardcore Mode"));
if(room==rm_scoreboard_co)draw_text(x-1, y+3, string_hash_to_newline("Comet Madness"));